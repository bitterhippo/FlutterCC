import 'package:flutter/material.dart';

void main() {
  runApp();
}

class MyApp extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text('This is my app'),
    );
  }
}
